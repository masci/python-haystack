Package name transition
#######################

This package has the old name `haystack` and its sole purpose is to
pull in the new package `python-haystack` so that downstream project
will have the time to update their requirements files and installation
scripts.